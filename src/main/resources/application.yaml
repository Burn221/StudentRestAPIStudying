spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/student_db
    username: postgres
    password: 1234


  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    database: POSTGRESQL

    properties:
      hibernate:
        format_sql: true
